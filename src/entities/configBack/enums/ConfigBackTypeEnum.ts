export const enum ConfigBackTypeEnum {
    postgresql = "postgresql",
    returns = "returns",
    validate = "validate",
    block = "block",
    convert = "convert",
    email = "email",
    config_link = "config_link",
    parsing_html = "parsing_html",
    file = "file",
    excel = "excel",
    foreach = "foreach"
}